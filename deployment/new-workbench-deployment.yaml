kind: Deployment
apiVersion: apps/v1
metadata:
  name: workbench
spec:
  replicas: 1
  selector:
    matchLabels:
      app: workbench
  template:
    metadata:
      labels:
        app: workbench
    spec:
      containers:
      - name: workbench-container
        image: gsiachamis/workbench:3.0
        imagePullPolicy: Always
        resources:
          requests:
            cpu: 4000m
            memory: 4000Mi
          limits:
            cpu: 5000m
            memory: 5000Mi
        env:
        args: ["mvn", "exec:java", "-Dexec.mainClass=ch.ethz.systems.strymon.ds2.flink.nexmark.sources.BidPersonGeneratorKafka", "-Dexec.args= --enable-bids-topic true --enable-auction-topic true --enable-person-topic true --load-pattern 'random' --use-default-configuration false --generator-parallelism 4 --initial-input-rate 10000000 --min-divergence 0 --max-divergence 0"]
